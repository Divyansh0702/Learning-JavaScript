"use strict"; // Treat all JS code as a newer version
// alert("Hello World") // It throws error as we are using node.js, not the browser

/*  DATA TYPES : 
    number --> 2 to the power 53
    bigint --> for storing very large value
    string --> " "/' '
    boolean --> by using "is" before the name of the variable, we can check if the value is "true/false"
    undefined --> var declared but it does not contain any value (It is a data type and 
    also it throws the value --> undefined)
    null --> empty (it is an object)
    Symbol --> uniqueness (function)
    object
*/

let myname = "divyansh"
let age = 15
let isLoggedIn = false


console.log(typeof 3); // number
console.log(typeof myname); //string
console.log(typeof age); // number
console.log(typeof Symbol); //function
console.log(typeof isLoggedIn); //boolean
console.log(typeof undefined); // undefined
console.log(typeof null); // object
console.log(typeof city); //undefined




